# URL absoluta del backend, solo necesaria para builds nativas (Capacitor).
# En web queda vacía: dev usa el proxy de Vite y prod sirve API y app juntas.
# Ejemplo: VITE_API_URL=https://jeshvonhanefesh.up.railway.app
VITE_API_URL=
